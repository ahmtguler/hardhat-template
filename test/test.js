const { parseEther } = require("ethers/lib/utils");
const { ethers } = require("hardhat");
const { expect } = require("chai");

describe("Set up the platform",  function() {
    it("Should deploy the contracts", async function() {

    });
});